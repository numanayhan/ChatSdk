use_frameworks!

platform :ios, '10.0'

target 'ChatSdk_Example' do
  pod 'ChatSdk', :path => '../'

  target 'ChatSdk_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 2.2.0'
    
  end
end
